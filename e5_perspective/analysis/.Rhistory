filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
data$exclude_written
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
data$exclude_written
?filter
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
data |>
filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
table(p_data$simultaneous_or_flip)
prop.table(table(p_data$simultaneous_or_flip))
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
data |>
filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
#====================================================================================
#                               ANALYSIS
#====================================================================================
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
cohen.d(data$identity, data$cond_name)
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
#data |>
#  filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
colnames(final_sample)
colnames(data)
data$consistent
## Check consistency of written responses against choices
non_na_values <- consistent[!is.na(consistent)]
## Check consistency of written responses against choices
non_na_values <- data$consistent[!is.na(data$consistent)]
non_na_values
table(non_na_values)
table(non_na_values)[2]/sum(table(non_na_values))
tail(data)
table(data$cond)
table(non_na_values)
59/103
prop.table(table(p_data$simultaneous_or_flip))
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
#data |>
#  filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Check consistency of written responses against choices
non_na_values <- data$consistent[!is.na(data$consistent)]
table(non_na_values)[2]/sum(table(non_na_values))
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
#====================================================================================
#                               ANALYSIS
#====================================================================================
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
cohen.d(data$identity, data$cond_name)
table(p_data$simultaneous_or_flip)
prop.table(table(p_data$simultaneous_or_flip))
data$simultaneous_or_flip
# ratings contingent upon choice
two_perspectives <- subset(p_data, data$simultaneous_or_flip == "Two consciousnesses")
two_perspectives$cond_name
mean(two_perspectives$identity)
two_perspectives$identity
# ratings contingent upon choice
two_perspectives <- subset(p_data, data$simultaneous_or_flip == "Two consciousnesses")
two_perspectives
# ratings contingent upon choice
two_perspectives <- subset(p_data, p_data$simultaneous_or_flip == "Two consciousnesses")
two_perspectives
mean(two_perspectives$identity)
p_data$simultaneous_or_flip
# ratings contingent upon choice
two_perspectives <- subset(p_data, p_data$simultaneous_or_flip == "Two consciousnesses")
mean(two_perspectives$identity)
single_flipping <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, flipping")
mean(single_flipping$identity)
single_split <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, split screen")
mean(single_split$identity)
single_simultaneous <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, simultaneous")
mean(single_simultaneous$identity)
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
data |>
filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Check consistency of written responses against choices
non_na_values <- data$consistent[!is.na(data$consistent)]
table(non_na_values)[2]/sum(table(non_na_values))
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
#====================================================================================
#                               ANALYSIS
#====================================================================================
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
cohen.d(data$identity, data$cond_name)
table(p_data$simultaneous_or_flip)
prop.table(table(p_data$simultaneous_or_flip))
# ratings contingent upon choice
two_perspectives <- subset(p_data, p_data$simultaneous_or_flip == "Two consciousnesses")
mean(two_perspectives$identity)
single_flipping <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, flipping")
mean(single_flipping$identity)
single_split <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, split screen")
mean(single_split$identity)
single_simultaneous <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, simultaneous")
mean(single_simultaneous$identity)
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
#data |>
#  filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Check consistency of written responses against choices
non_na_values <- data$consistent[!is.na(data$consistent)]
table(non_na_values)[2]/sum(table(non_na_values))
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
#====================================================================================
#                               ANALYSIS
#====================================================================================
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
cohen.d(data$identity, data$cond_name)
table(p_data$simultaneous_or_flip)
prop.table(table(p_data$simultaneous_or_flip))
# ratings contingent upon choice
two_perspectives <- subset(p_data, p_data$simultaneous_or_flip == "Two consciousnesses")
mean(two_perspectives$identity)
mean(two_perspectives$identity)
sd(two_perspectives$identity)
single_split <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, split screen")
mean(single_split$identity)
sd(single_split$identity)
single_flipping <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, flipping")
mean(single_flipping$identity)
sd(single_flipping$identity)
mean(single_simultaneous$identity)
single_simultaneous <- subset(p_data, p_data$simultaneous_or_flip == "Single consciousness, simultaneous")
mean(single_simultaneous$identity)
sd(single_simultaneous$identity)
#====================================================================================
#                               Self Clone
#====================================================================================
## clear workspace
rm(list = ls())
## install packages
if (!require(pacman)) {install.packages("pacman")}
pacman::p_load('rjson',
'ltm',
'heplots',
'lmtest',
'compute.es',
'ltm',
'lsr',
'lsmeans',
'nnet',
'mlogit',
'dplyr',
'effsize',
'ggplot'
)
#====================================================================================
#                             PRE-PROCESSING
#====================================================================================
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
data <- read.csv("data_label.csv")
data <- data[-c(1,2),]
## Attention Checks
data |>
filter(attn_1 == "Paul" & attn_2 == "Purple") -> data
## Number of Participants
initial_sample <- nrow(data); initial_sample
## Comprehension Checks
data |>
filter(
comp_q == "Teletransportation"
) -> data
## Also exclude those who wrote nonsense
data |>
filter(exclude_written == "0" | exclude_written == "3") -> data
## Final Sample
final_sample <- nrow(data); final_sample
## Check consistency of written responses against choices
non_na_values <- data$consistent[!is.na(data$consistent)]
table(non_na_values)[2]/sum(table(non_na_values))
## Organize
data <- data %>%
mutate(identity = as.numeric(ifelse(basic_identity_1 != "", basic_identity_1, first_identity_1)))
data$cond_name <- ifelse(data$cond==1, 'basic', 'perspective')
## Demographics
mean(as.numeric(data$age))
table(data$gender)
table(data$cond)
#====================================================================================
#                               ANALYSIS
#====================================================================================
p_data <- subset(data, data$cond_name=='perspective')
t.test(data$identity ~ data$cond_name)
aggregate(data$identity ~ data$cond_name, data = data, FUN = sd)
cohen.d(data$identity, data$cond_name)
table(p_data$simultaneous_or_flip)
prop.table(table(p_data$simultaneous_or_flip))
